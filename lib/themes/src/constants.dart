part of '../theme.dart';

abstract class AppColors {
  static const bg = Color(0xffF6F6F6);
  static const appbar = Color.fromARGB(255, 16, 2, 2);
  static const buttons = Colors.purpleAccent;
  static const primary = Colors.blueAccent;
}
